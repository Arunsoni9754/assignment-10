#include<stdio.h>
float area (float);
float main()
{float a=1,r=0;  
printf("Enter the radius of circle ");
    scanf("%f",&r);
    printf("the area is %f",area(r));
    return 0;
}
float area(float a)
{
    return (3.14*a*a);
}
